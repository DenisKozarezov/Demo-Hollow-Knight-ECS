namespace BehaviourTree.Runtime
{
    public enum PortConnection : byte
    {
        /// <summary> Allows only single connection</summary>
        Single,
        /// <summary> Allows multiple connections</summary>
        Multi,
    }
}
